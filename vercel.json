{
  "buildCommand": "npm install --prefix frontend && npx prisma generate --schema frontend/prisma/schema.prisma && npm run build --prefix frontend",
  "outputDirectory": "frontend/.next",
  "framework": "nextjs"
}
